# Core Django and REST Framework
Django==5.2.4
djangorestframework==3.15.2
django-cors-headers==4.3.1

# Database (SQLite is included with Django, but PostgreSQL is recommended for production)
# psycopg2-binary==2.9.9  # Uncomment if using PostgreSQL

# Development server (optional, Django's built-in server is sufficient for development)
# gunicorn==21.2.0  # Uncomment for production deployment

# Environment variables (optional but recommended)
python-dotenv==1.0.0

# HTTP requests (for external APIs like geocoding)
requests==2.31.0 